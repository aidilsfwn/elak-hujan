import type { UserConfig } from '@/types/config';
const days = [{ key: 'monday', label: 'Isn' }, { key: 'tuesday', label: 'Sel' }, { key: 'wednesday', label: 'Rab' }, { key: 'thursday', label: 'Kha' }, { key: 'friday', label: 'Jum' }];
export function StepDays({ draft, onUpdate }: { draft: Partial<UserConfig>; onUpdate: (value: Partial<UserConfig>) => void }) { const count = draft.officeDaysPerWeek ?? 3; const preferred = draft.preferredDays ?? days.map((day) => day.key); return <div className="setup-days"><div><span>Bilangan hari seminggu</span><div>{[1,2,3,4,5].map((number) => <button type="button" className={count === number ? 'is-selected' : ''} key={number} onClick={() => onUpdate({ officeDaysPerWeek: number })}>{number}</button>)}</div></div><div><span>Hari yang diutamakan</span><div>{days.map((day) => { const selected = preferred.includes(day.key); return <button type="button" className={selected ? 'is-selected' : ''} key={day.key} onClick={() => onUpdate({ preferredDays: selected ? preferred.filter((item) => item !== day.key) : [...preferred, day.key] })}>{day.label}</button>; })}</div>{preferred.length < count && <p className="field-error">Pilih sekurang-kurangnya {count} hari.</p>}</div></div>; }
